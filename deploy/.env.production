# Next.js production env (non-secret placeholders only)
# Slot mapping: Next #1 -> revindennis.cloud -> PORT 3000

NODE_ENV=production
PORT=3000

### Gmail / Google Workspace SMTP (server-side only; configure in Vercel)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=boaedan11555@gmail.com
SMTP_PASS=ivmriajfiwalnrtp

### Contact mail routing
# Where contact form submissions should arrive (an email you own).
CONTACT_TO_EMAIL=revinfreelance@gmail.com